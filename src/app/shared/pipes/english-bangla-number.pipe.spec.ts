import { EnglishBanglaNumberPipe } from './english-bangla-number.pipe';

describe('EnglishBanglaNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new EnglishBanglaNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
